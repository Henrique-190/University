package model;

public class Admin extends User {
	public Admin(String address, String email, String name, String password, String username, String phone_number) {
		super(address, email, name, password, username, phone_number);
	}
}